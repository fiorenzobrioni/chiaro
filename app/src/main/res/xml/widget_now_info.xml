<?xml version="1.0" encoding="utf-8"?>
<!-- The Now widget (VISION §5.9): icon, temperature, place. updatePeriodMillis is 0
     on purpose — the shared periodic job drives every repaint, at the reader's own
     update frequency, instead of a second clock the battery pays for. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_now_desc"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
