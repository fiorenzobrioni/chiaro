<?xml version="1.0" encoding="utf-8"?>
<!-- The day's low. `ic_range_high` turned over: same geometry, same weight, same caps, so
     the pair reads as one statement with two directions rather than as two drawings that
     happen to point different ways. See that file for why it is a drawing at all. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M12,4.5 L12,18.5 M5.5,12 L12,18.5 L18.5,12"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="2.4" />
</vector>
